# .github/workflows/docs-widoco.yml

name: Docs (WIDOCO)
on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  widoco:
    uses: Airy59/OntoQA/.github/workflows/docs-widoco.yml@main
    # main_ontology + reasoner are resolved from ontology/src/config.md (YAML front matter)
